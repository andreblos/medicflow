spring.application.name=medicflow

# Conexão com o PostgreeSQL
spring.datasource.url=jdbc:postgreesql://localhost:5432/medicflow
spring.datasource.username=Andre
spring.datasource.password=darinha82

# Configuração do JPA(Hibernate)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

#Driver do PostgreeSQL
spring.datasource.hikari.driver-class-name=org.postgreesql.Driver

# Inicialização do banco
spring.sql.init.mode=always
